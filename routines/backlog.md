# 待辦：程式碼與閘門的改動

每日那一趟每天會挑一件今天實際碰到的摩擦去優化。改 routine 指示與改資料它可以
自己做，**動 `index.html`、`scripts/`、`.github/` 的，寫到這裡，週日那趟做。**

為什麼要分開：資料錯了下一次覆蓋就好，程式碼壞了整個網站打不開。
每日那趟跑在交易日早上、時間壓力最大，不適合做會改變行為的東西。

## 寫法

一筆一行，最前面加，格式：

```
- [ ] YYYY-MM-DD ｜ 遇到的摩擦（一句，要具體）｜ 建議怎麼改 ｜ 誰提的（daily／review／intraday）
```

**必須是真的碰到的問題**，不是「感覺可以更好」。看不出摩擦在哪的條目，
週日那趟會直接刪掉——描述不清楚的改動做出來只會更亂。

週日那趟：做掉幾筆就把那幾行刪掉（做完才刪），沒做的留著並在回報裡說為什麼。
一次不要做超過兩筆，兩道閘門要全過才算數。

## 目前待辦

- [ ] 2026-09-24 ｜ `mergeRemote`（index.html 1862 行附近）對「curated 更正的目標事件與原本要下架的舊事件，兩個日期都已經是過去」的情況不會去重：`if(e.date<from)continue;` 只要舊事件（e）的日期已經早於今天就直接跳過，不管新舊兩筆是不是同一件事——實測重現：AVGO 財報 baseline 生成 2026-09-10（est:false），curated.json 更正到 2026-09-02（est:false），兩者標題相同、相差 8 天在 10 天門檻內，理應二選一去重，但因為執行時「今天」已經超過 09-10，`e.date<from` 為真直接 continue，於是兩筆永久並存在事件列表裡。複盤 09-24 那趟已經撞到並跳過這筆（見 changelog 09-24 review 條目）。這條 guard 原本是為了保護「已經發生的歷史事件不要被改」，但擋錯了對象：應該只在 `f`（curated 給的新資料）也已經過去、且它跟 `e` 不是同一件事時才跳過，而不是只要 `e` 過去就整個放棄比對。｜ 把 `if(e.date<from)continue;` 改成只在確定不是同一事件時才生效，或者把「兩者皆已成為過去」單獨判斷成一種「歷史更正」情境，仍然執行去重（但不要讓它去動真正不相關的歷史事件）；改完要跑一次上面 AVGO 這個具體 repro 案例驗證 ｜ daily
- [ ] 2026-09-14 ｜ 複盤停擺兩週沒有人發現，因為「跑了但沒事」跟「根本沒跑」在 repo
      裡留下的痕跡一模一樣——都是沒有 commit ｜ 四個 routine 每趟結束時在
      `data/runs.json` 寫一行心跳（任務名、時間、有沒有變動、五項檢查的結果），
      資料頁顯示最近一次；空轉就會自己現形。要動 `index.html` 與 `validate_data.py`
      ｜ daily
- [ ] 2026-09-14 ｜ `reviews.json` 的 `cons`／`act` 沒有單位欄位，台積電月營收那筆
      只能寫 `"4800"`／`"5148.06"`，單位（億元）只好塞進 `metrics`，畫面上那兩個
      數字是裸的 ｜ 給 reviews 的 item 加 `unit`（跟 `priced.json` 一致），
      顯示時接在數字後面 ｜ review
- [ ] 2026-09-14 ｜ 台灣端事件的 `sigma` 必須用台股口徑，但驗證器分不出來那個數字
      是美股還是台股算的，填錯不會有人知道 ｜ 規則是「有 `twse` 而 `spx`／`ndx` 全空
      → `sigma` 應該接近台股的 20 日波動」，可以在 `validate_data.py` 直接用
      `px.json` 自己算一次做合理性檢查，差兩倍以上就警告 ｜ daily
